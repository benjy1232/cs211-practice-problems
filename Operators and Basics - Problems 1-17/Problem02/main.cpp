#include<iostream>
using namespace std;

int main(void) {
	int x,y;
	cout << "Please enter a number: ";
	cin >> x;
	y = x * 2;
	cout << "The number " << y << " is double the value of your input number " << x;
	return 0;
	//<datatype> <name> = [value];
}

//Possible datatypes
//integer data - declared as int
//decimal data - declared as float or double
//characters - char - can only hold a single character
